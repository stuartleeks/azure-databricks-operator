# This patch is used to apply namespace selector injection to the service monitor
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: controller-manager-metrics-service
  labels:
    release: prom-azure-databricks-operator
spec:
  namespaceSelector:
    matchNames:
      - $(MetricsService.Namespace)